# Структура проекта

## 📁 Обзор структуры

```
todo-telegram-app/
├── assets/
│   └── css/
│       └── main.css              # Главные стили с Tailwind
├── components/
│   ├── TodoApp.vue               # Главный компонент приложения
│   ├── TodoItem.vue              # Компонент отдельной задачи
│   ├── TodoFilters.vue           # Компонент фильтров
│   ├── TodoModal.vue             # Модальное окно создания/редактирования задачи
│   ├── PrioritySelector.vue      # Компонент выбора приоритета
│   ├── ProjectSelector.vue       # Компонент выбора проекта
│   └── DateSelector.vue          # Компонент выбора даты
├── composables/
│   ├── useTodos.ts               # Composable для работы с задачами
│   └── useProjects.ts            # Composable для работы с проектами
├── plugins/
│   └── telegram.client.ts        # Плагин для инициализации Telegram Web App
├── server/
│   └── plugins/
│       └── scheduler.ts          # Встроенный планировщик задач для уведомлений
├── server/
│   ├── api/
│   │   ├── auth/
│   │   │   └── telegram.post.ts  # Авторизация через Telegram
│   │   ├── todos/
│   │   │   ├── [id].delete.ts    # Удаление задачи
│   │   │   ├── [id].put.ts       # Обновление задачи
│   │   │   ├── clear-completed.delete.ts  # Очистка выполненных
│   │   │   ├── index.get.ts      # Получение всех задач
│   │   │   └── index.post.ts     # Создание задачи
│   │   ├── projects/
│   │   │   ├── [id].delete.ts    # Удаление проекта
│   │   │   ├── [id].put.ts       # Обновление проекта
│   │   │   ├── index.get.ts      # Получение всех проектов
│   │   │   └── index.post.ts     # Создание проекта
│   │   ├── notifications/
│   │   │   ├── daily.post.ts     # Ежедневные уведомления
│   │   │   └── send.post.ts      # Отправка уведомлений
│   │   └── auth/
│   │       └── telegram.post.ts # Авторизация через Telegram
│   └── utils/
│       └── db.ts                 # Утилиты для работы с БД
├── types/
│   └── todo.ts                   # TypeScript типы
├── app.vue                       # Корневой компонент
├── nuxt.config.ts                # Конфигурация Nuxt
├── tailwind.config.js            # Конфигурация Tailwind
├── postcss.config.js             # Конфигурация PostCSS
├── tsconfig.json                 # Конфигурация TypeScript
├── package.json                  # Зависимости
├── README.md                     # Документация
├── DEPLOY.md                     # Инструкция по развертыванию
└── .env.example                  # Пример файла окружения
```

## 🔧 Основные компоненты

### Components

- **TodoApp.vue** - Главный компонент, содержащий:
  - Header с названием и счетчиком активных задач
  - Кнопку добавления новой задачи (открывает модальное окно)
  - Список задач
  - Фильтры
  - Обработку авторизации
  - Модальное окно для создания/редактирования задач

- **TodoItem.vue** - Компонент отдельной задачи:
  - Чекбокс для отметки выполнения
  - Отображение названия задачи
  - Отображение описания (если есть)
  - Индикатор приоритета (иконка флага с цветом)
  - Отображение проекта (с цветовой индикацией)
  - Отображение даты выполнения (с умным форматированием)
  - Подсветка просроченных задач красным цветом
  - Кнопки редактирования и удаления
  - Клик по задаче открывает модальное окно для редактирования

- **TodoFilters.vue** - Компонент фильтров:
  - Фильтрация (все/активные/выполненные)
  - Кнопка очистки выполненных задач

- **TodoModal.vue** - Модальное окно для создания/редактирования задач:
  - Поле ввода названия задачи
  - Поле ввода описания
  - Выбор приоритета (PrioritySelector)
  - Выбор проекта (ProjectSelector)
  - Выбор даты выполнения (DateSelector)
  - Кнопки сохранения и отмены

- **PrioritySelector.vue** - Компонент выбора приоритета:
  - 4 варианта: Нет, Низкий, Средний, Высокий
  - Визуальная индикация выбранного приоритета

- **ProjectSelector.vue** - Компонент выбора проекта:
  - Выпадающий список проектов
  - Цветовая индикация проектов
  - Опция "Без проекта"

- **DateSelector.vue** - Компонент выбора даты:
  - Быстрые опции: Сегодня, Завтра, Через неделю
  - Календарь для выбора произвольной даты
  - Кнопка очистки даты

### Composables

- **useTodos.ts** - Composable для управления задачами:
  - Получение задач (с информацией о проектах)
  - Создание задачи (с поддержкой описания, приоритета, проекта, даты)
  - Обновление задачи (все поля)
  - Удаление задачи
  - Очистка выполненных задач
  - Фильтрация задач (все/активные/выполненные)
  - Computed свойства: filteredTodos, activeCount, completedTodos

- **useProjects.ts** - Composable для управления проектами:
  - Получение проектов пользователя
  - Создание проекта
  - Обновление проекта
  - Удаление проекта

- **useNotifications.ts** - Composable для управления настройками уведомлений:
  - Получение настроек уведомлений
  - Обновление настроек уведомлений
  - Отправка тестового уведомления

### Server API

#### Todos API
- `GET /api/todos` - Получить все задачи пользователя
  - Сортировка: по приоритету (high → medium → low → none), затем по дате выполнения, затем по дате создания
  - Возвращает задачи с информацией о проектах (JOIN)
- `POST /api/todos` - Создать новую задачу
  - Тело запроса: `{ text: string, description?: string, priority?: 'none'|'low'|'medium'|'high', projectId?: string, dueDate?: Date }`
  - Если `projectId` не указан, задача привязывается к проекту "Inbox"
- `PUT /api/todos/:id` - Обновить задачу
  - Тело запроса: `{ text?: string, description?: string, completed?: boolean, priority?: 'none'|'low'|'medium'|'high', projectId?: string, dueDate?: Date }`
- `DELETE /api/todos/:id` - Удалить задачу
- `DELETE /api/todos/clear-completed` - Очистить выполненные задачи

#### Projects API
- `GET /api/projects` - Получить все проекты пользователя
  - Сортировка: по названию (ASC)
- `POST /api/projects` - Создать новый проект
  - Тело запроса: `{ name: string, color?: string }`
  - Проверка на уникальность названия для пользователя
- `PUT /api/projects/:id` - Обновить проект
  - Тело запроса: `{ name?: string, color?: string }`
- `DELETE /api/projects/:id` - Удалить проект
  - Запрещено удаление проекта "Inbox"
  - При удалении проекта все его задачи переносятся в "Inbox"

#### Auth API
- `POST /api/auth/telegram` - Авторизация через Telegram

#### Notifications API
- `POST /api/notifications/send` - Отправить произвольное уведомление пользователю
  - Тело запроса: `{ userId: number, message: string }`
- `POST /api/notifications/daily` - Отправить ежедневные уведомления о задачах на сегодня
  - Учитывает настройки пользователей (notificationsEnabled, dailyNotifications)
  - Отправляет только пользователям с включенными уведомлениями
- `POST /api/notifications/reminders` - Отправить напоминания о задачах за N дней до дедлайна
  - Использует настройки `reminderDaysBefore` каждого пользователя
  - По умолчанию: напоминания за 1 и 3 дня
- `POST /api/notifications/overdue` - Отправить уведомления о просроченных задачах
  - Учитывает настройку `notifyOnOverdue` пользователя

#### Settings API
- `GET /api/settings` - Получить настройки уведомлений пользователя
- `PUT /api/settings` - Обновить настройки уведомлений пользователя
  - Тело запроса: `{ notificationsEnabled?: boolean, dailyNotifications?: boolean, dailyNotificationTime?: string, reminderDaysBefore?: number[], notifyOnCreate?: boolean, notifyOnUpdate?: boolean, notifyOnOverdue?: boolean, timezone?: string }`

### Database

База данных автоматически инициализируется при первом запуске:

#### Таблица `users`
- `id` (BIGINT) - ID пользователя Telegram (Primary Key)
- `first_name` (VARCHAR(255)) - Имя
- `last_name` (VARCHAR(255)) - Фамилия
- `username` (VARCHAR(255)) - Username
- `language_code` (VARCHAR(10)) - Код языка
- `is_premium` (BOOLEAN) - Premium статус
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

#### Таблица `projects`
- `id` (UUID) - ID проекта (Primary Key)
- `user_id` (BIGINT) - ID пользователя (Foreign Key, ссылается на users.id)
- `name` (VARCHAR(255)) - Название проекта
- `color` (VARCHAR(7)) - Цвет проекта (hex формат, по умолчанию '#2481cc')
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления
- **Уникальный индекс**: `(user_id, name)` - уникальное название проекта для каждого пользователя

**Примечание**: При создании пользователя автоматически создается проект "Inbox" с цветом '#2481cc'.

#### Таблица `todos`
- `id` (UUID) - ID задачи (Primary Key)
- `user_id` (BIGINT) - ID пользователя (Foreign Key, ссылается на users.id)
- `project_id` (UUID) - ID проекта (Foreign Key, ссылается на projects.id, nullable)
- `text` (TEXT) - Текст задачи (обязательное поле)
- `description` (TEXT) - Описание задачи (nullable)
- `completed` (BOOLEAN) - Статус выполнения (по умолчанию false)
- `priority` (VARCHAR(10)) - Приоритет задачи (CHECK: 'none', 'low', 'medium', 'high', по умолчанию 'none')
- `due_date` (TIMESTAMP) - Дата выполнения (nullable)
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

**Примечание**: Если `project_id` не указан при создании задачи, она автоматически привязывается к проекту "Inbox" пользователя.

#### Таблица `user_settings`
- `user_id` (BIGINT) - ID пользователя (Primary Key, Foreign Key, ссылается на users.id)
- `notifications_enabled` (BOOLEAN) - Включены ли уведомления (по умолчанию TRUE)
- `daily_notifications` (BOOLEAN) - Ежедневные уведомления (по умолчанию TRUE)
- `daily_notification_time` (TIME) - Время ежедневных уведомлений (по умолчанию '09:00:00')
- `reminder_days_before` (INTEGER[]) - Массив дней до дедлайна для напоминаний (по умолчанию ARRAY[1, 3])
- `notify_on_create` (BOOLEAN) - Уведомлять при создании задачи (по умолчанию FALSE)
- `notify_on_update` (BOOLEAN) - Уведомлять при выполнении задачи (по умолчанию FALSE)
- `notify_on_overdue` (BOOLEAN) - Уведомлять о просроченных задачах (по умолчанию TRUE)
- `timezone` (VARCHAR(50)) - Часовой пояс пользователя (по умолчанию 'UTC')
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

**Примечание**: При создании пользователя автоматически создаются настройки по умолчанию.

#### Индексы

**Для таблицы `todos`:**
- `idx_todos_user_id` - Индекс по user_id (для быстрого поиска задач пользователя)
- `idx_todos_completed` - Индекс по completed (для фильтрации выполненных задач)
- `idx_todos_due_date` - Индекс по due_date (для уведомлений и сортировки)
- `idx_todos_priority` - Индекс по priority (для сортировки по приоритету)
- `idx_todos_project_id` - Индекс по project_id (для фильтрации по проектам)

**Для таблицы `projects`:**
- `idx_projects_user_id` - Индекс по user_id (для быстрого поиска проектов пользователя)

**Для таблицы `user_settings`:**
- `idx_user_settings_notifications` - Составной индекс по notifications_enabled и daily_notifications (для фильтрации при отправке уведомлений)

#### Связи между таблицами

```
users (1) ──< (N) projects
users (1) ──< (N) todos
projects (1) ──< (N) todos
```

- Один пользователь может иметь множество проектов
- Один пользователь может иметь множество задач
- Один проект может содержать множество задач
- При удалении пользователя каскадно удаляются все его проекты и задачи
- При удалении проекта задачи не удаляются, а `project_id` устанавливается в NULL (ON DELETE SET NULL)

## 🎨 Стилизация

### Tailwind CSS
- Использование Telegram theme colors через CSS переменные
- Адаптивный дизайн
- Темная/светлая тема (автоматически из Telegram)

### Telegram Theme Colors
- `--tg-theme-bg-color` - Цвет фона
- `--tg-theme-text-color` - Цвет текста
- `--tg-theme-hint-color` - Цвет подсказок
- `--tg-theme-link-color` - Цвет ссылок
- `--tg-theme-button-color` - Цвет кнопок
- `--tg-theme-button-text-color` - Цвет текста кнопок
- `--tg-theme-secondary-bg-color` - Вторичный цвет фона

## 🔐 Безопасность

- Все API endpoints требуют заголовок `x-telegram-user-id`
- Данные пользователя проверяются на сервере
- Использование HTTPS обязательно в продакшене
- Чувствительные данные хранятся в переменных окружения

## 📱 Интеграция с Telegram

- Автоматическая инициализация Telegram Web App SDK
- Получение данных пользователя из `initDataUnsafe`
- Применение темы Telegram
- Haptic feedback для улучшения UX
- Поддержка темной/светлой темы

