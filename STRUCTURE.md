# Структура проекта

## 📁 Обзор структуры

```
todo-telegram-app/
├── assets/
│   └── css/
│       └── main.css              # Главные стили с Tailwind
├── components/
│   ├── TodoApp.vue               # Главный компонент приложения
│   ├── TodoItem.vue              # Компонент отдельной задачи
│   └── TodoFilters.vue           # Компонент фильтров
├── composables/
│   └── useTodos.ts               # Composable для работы с задачами
├── plugins/
│   └── telegram.client.ts        # Плагин для инициализации Telegram Web App
├── server/
│   ├── api/
│   │   ├── auth/
│   │   │   └── telegram.post.ts  # Авторизация через Telegram
│   │   ├── notifications/
│   │   │   ├── daily.post.ts     # Ежедневные уведомления
│   │   │   └── send.post.ts      # Отправка уведомлений
│   │   └── todos/
│   │       ├── [id].delete.ts    # Удаление задачи
│   │       ├── [id].put.ts       # Обновление задачи
│   │       ├── clear-completed.delete.ts  # Очистка выполненных
│   │       ├── index.get.ts      # Получение всех задач
│   │       └── index.post.ts     # Создание задачи
│   └── utils/
│       └── db.ts                 # Утилиты для работы с БД
├── types/
│   └── todo.ts                   # TypeScript типы
├── app.vue                       # Корневой компонент
├── nuxt.config.ts                # Конфигурация Nuxt
├── tailwind.config.js            # Конфигурация Tailwind
├── postcss.config.js             # Конфигурация PostCSS
├── tsconfig.json                 # Конфигурация TypeScript
├── package.json                  # Зависимости
├── README.md                     # Документация
├── DEPLOY.md                     # Инструкция по развертыванию
└── .env.example                  # Пример файла окружения
```

## 🔧 Основные компоненты

### Components

- **TodoApp.vue** - Главный компонент, содержащий:
  - Header с названием и счетчиком задач
  - Форму добавления новых задач
  - Список задач
  - Фильтры
  - Обработку авторизации

- **TodoItem.vue** - Компонент отдельной задачи:
  - Чекбокс для отметки выполнения
  - Редактирование текста (двойной клик)
  - Удаление задачи
  - Отображение даты выполнения (если задана)

- **TodoFilters.vue** - Компонент фильтров:
  - Фильтрация (все/активные/выполненные)
  - Кнопка очистки выполненных задач

### Composables

- **useTodos.ts** - Composable для управления задачами:
  - Получение задач
  - Создание задачи
  - Обновление задачи
  - Удаление задачи
  - Очистка выполненных задач
  - Фильтрация задач

### Server API

#### Todos API
- `GET /api/todos` - Получить все задачи пользователя
- `POST /api/todos` - Создать новую задачу
- `PUT /api/todos/:id` - Обновить задачу
- `DELETE /api/todos/:id` - Удалить задачу
- `DELETE /api/todos/clear-completed` - Очистить выполненные задачи

#### Auth API
- `POST /api/auth/telegram` - Авторизация через Telegram

#### Notifications API
- `POST /api/notifications/send` - Отправить уведомление пользователю
- `POST /api/notifications/daily` - Отправить ежедневные уведомления

### Database

База данных автоматически инициализируется при первом запуске:

#### Таблица `users`
- `id` (BIGINT) - ID пользователя Telegram (Primary Key)
- `first_name` (VARCHAR) - Имя
- `last_name` (VARCHAR) - Фамилия
- `username` (VARCHAR) - Username
- `language_code` (VARCHAR) - Код языка
- `is_premium` (BOOLEAN) - Premium статус
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

#### Таблица `todos`
- `id` (UUID) - ID задачи (Primary Key)
- `user_id` (BIGINT) - ID пользователя (Foreign Key)
- `text` (TEXT) - Текст задачи
- `completed` (BOOLEAN) - Статус выполнения
- `due_date` (TIMESTAMP) - Дата выполнения
- `created_at` (TIMESTAMP) - Дата создания
- `updated_at` (TIMESTAMP) - Дата обновления

#### Индексы
- `idx_todos_user_id` - Индекс по user_id
- `idx_todos_completed` - Индекс по completed
- `idx_todos_due_date` - Индекс по due_date

## 🎨 Стилизация

### Tailwind CSS
- Использование Telegram theme colors через CSS переменные
- Адаптивный дизайн
- Темная/светлая тема (автоматически из Telegram)

### Telegram Theme Colors
- `--tg-theme-bg-color` - Цвет фона
- `--tg-theme-text-color` - Цвет текста
- `--tg-theme-hint-color` - Цвет подсказок
- `--tg-theme-link-color` - Цвет ссылок
- `--tg-theme-button-color` - Цвет кнопок
- `--tg-theme-button-text-color` - Цвет текста кнопок
- `--tg-theme-secondary-bg-color` - Вторичный цвет фона

## 🔐 Безопасность

- Все API endpoints требуют заголовок `x-telegram-user-id`
- Данные пользователя проверяются на сервере
- Использование HTTPS обязательно в продакшене
- Чувствительные данные хранятся в переменных окружения

## 📱 Интеграция с Telegram

- Автоматическая инициализация Telegram Web App SDK
- Получение данных пользователя из `initDataUnsafe`
- Применение темы Telegram
- Haptic feedback для улучшения UX
- Поддержка темной/светлой темы

