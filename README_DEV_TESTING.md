# 🧪 Тестирование в Dev режиме - Краткая инструкция

## Быстрый старт

### 1. Запуск тестов во время разработки

```bash
# Терминал 1: Запустить dev сервер
npm run dev

# Терминал 2: Запустить тесты в watch режиме
npm run test:watch
```

### 2. Тестирование компонентов в браузере

1. Откройте `http://localhost:3002/dev/components`
2. Используйте интерактивную страницу для тестирования всех компонентов
3. Все изменения видны в реальном времени

### 3. Использование Dev утилит

В консоли браузера (F12) доступны утилиты:

```javascript
// Создать тестовую задачу
__DEV_TEST_UTILS__.createTestTodo({ text: 'Тест', priority: 'high' })

// Логировать состояние
__DEV_TEST_UTILS__.logTodos(todosArray)

// Очистить хранилище
__DEV_TEST_UTILS__.clearStorage()
```

## Доступные команды

| Команда | Описание |
|---------|----------|
| `npm run test:watch` | Тесты в watch режиме (автоперезапуск) |
| `npm run test:ui` | Тесты с UI интерфейсом |
| `npm run test:dev:coverage` | Тесты с покрытием кода |
| `npm run test:e2e:ui` | E2E тесты с UI |

## Полезные ссылки

- 📖 [DEV_TESTING.md](./DEV_TESTING.md) - Полное руководство
- 📖 [TESTING.md](./TESTING.md) - Общее руководство по тестированию
- 🎨 `/dev/components` - Страница для тестирования компонентов

