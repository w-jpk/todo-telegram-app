# План реализации функционала Премиум участника

## 📋 Анализ текущего состояния

### Что уже есть:
- ✅ Поле `is_premium` в таблице `users` (база данных)
- ✅ Поле `is_premium` в типе `TelegramUser` (TypeScript)
- ✅ Статический текст "Премиум участник" в UI (строка 90 в `pages/settings/index.vue`)
- ✅ Синхронизация статуса из Telegram при авторизации

### Что нужно реализовать:
- ❌ Логика проверки премиум статуса
- ❌ Ограничения для бесплатных пользователей
- ❌ UI для отображения статуса (динамический)
- ❌ Страница/модальное окно для получения премиум
- ❌ Валидация ограничений на бэкенде
- ❌ Интеграция с Telegram Premium API (если доступна)

---

## 🎯 Ограничения для бесплатных пользователей

### 1. Количество задач (Todos)
- **Бесплатно**: до 50 активных задач
- **Премиум**: без ограничений
- **Проверка**: при создании новой задачи

### 2. Количество проектов
- **Бесплатно**: до 5 проектов (включая "Inbox")
- **Премиум**: без ограничений
- **Проверка**: при создании нового проекта

### 3. Количество тегов
- **Бесплатно**: до 10 тегов
- **Премиум**: без ограничений
- **Проверка**: при создании нового тега

### 4. Подзадачи (Subtasks)
- **Бесплатно**: до 3 уровней вложенности
- **Премиум**: без ограничений
- **Проверка**: при создании подзадачи

### 5. Повторяющиеся задачи (Recurring Tasks)
- **Бесплатно**: до 5 повторяющихся задач
- **Премиум**: без ограничений
- **Проверка**: при создании повторяющейся задачи

### 6. Экспорт данных
- **Бесплатно**: только JSON, 1 раз в месяц
- **Премиум**: JSON, CSV, Todoist формат, без ограничений
- **Проверка**: при запросе экспорта

### 7. Резервное копирование
- **Бесплатно**: ручное, 1 раз в неделю
- **Премиум**: автоматическое ежедневное + ручное без ограничений
- **Проверка**: при настройке частоты резервного копирования

### 8. Настройки уведомлений
- **Бесплатно**: базовые уведомления (ежедневные, напоминания)
- **Премиум**: расширенные (тихие часы, кастомные напоминания, вибрации)
- **Проверка**: при изменении настроек уведомлений

### 9. Темы и внешний вид
- **Бесплатно**: светлая и темная тема
- **Премиум**: кастомные акцентные цвета, дополнительные темы
- **Проверка**: при выборе кастомного цвета

### 10. Статистика и аналитика
- **Бесплатно**: базовая статистика (количество задач, выполнено)
- **Премиум**: расширенная аналитика (графики, тренды, продуктивность)
- **Проверка**: при доступе к расширенной статистике

---

## 🔐 Способы получения Премиум статуса

### Вариант 1: Интеграция с Telegram Premium (рекомендуется)
- Использовать `is_premium` из Telegram WebApp API
- Автоматическая синхронизация статуса
- Пользователь получает премиум через Telegram

**Преимущества:**
- Уже есть поле в базе данных
- Не нужно реализовывать платежную систему
- Интеграция с экосистемой Telegram

**Реализация:**
```typescript
// Проверка статуса из Telegram
const isPremium = computed(() => {
  return $telegram?.user?.is_premium || false
})
```

### Вариант 2: Собственная система подписки
- Создать таблицу `subscriptions` в базе данных
- Интеграция с платежными системами (Stripe, PayPal, etc.)
- Управление подписками через админ-панель

**Структура таблицы:**
```sql
CREATE TABLE subscriptions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
  plan VARCHAR(20) NOT NULL CHECK (plan IN ('free', 'premium', 'trial')),
  status VARCHAR(20) NOT NULL CHECK (status IN ('active', 'cancelled', 'expired')),
  start_date TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
  end_date TIMESTAMP,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Вариант 3: Гибридный подход (рекомендуется для начала)
- Использовать Telegram Premium как основной способ
- Добавить возможность ручной активации премиум (для тестирования/админов)
- В будущем добавить собственную систему подписки

---

## 🏗️ Архитектура реализации

### 1. Backend (Server-side)

#### 1.1. Создать композабл для проверки премиум статуса
**Файл**: `server/utils/premium.ts`
```typescript
export async function checkPremiumStatus(userId: number): Promise<boolean> {
  // Проверка из базы данных
  // Можно добавить кеширование
}

export async function getUserLimits(userId: number) {
  const isPremium = await checkPremiumStatus(userId)
  return {
    maxTodos: isPremium ? Infinity : 50,
    maxProjects: isPremium ? Infinity : 5,
    maxTags: isPremium ? Infinity : 10,
    maxSubtaskLevels: isPremium ? Infinity : 3,
    maxRecurringTasks: isPremium ? Infinity : 5,
    // ... другие ограничения
  }
}
```

#### 1.2. Middleware для проверки ограничений
**Файл**: `server/middleware/premium-check.ts`
- Проверка лимитов перед созданием ресурсов
- Возврат понятных ошибок с информацией о лимитах

#### 1.3. Обновить API endpoints
- `POST /api/todos` - проверка лимита задач
- `POST /api/projects` - проверка лимита проектов
- `POST /api/tags` - проверка лимита тегов
- `GET /api/premium/status` - получение статуса премиум
- `GET /api/premium/limits` - получение текущих лимитов

### 2. Frontend (Client-side)

#### 2.1. Создать композабл `usePremium`
**Файл**: `composables/usePremium.ts`
```typescript
export const usePremium = () => {
  const isPremium = computed(() => {
    return $telegram?.user?.is_premium || false
  })
  
  const limits = ref({
    maxTodos: 50,
    maxProjects: 5,
    // ...
  })
  
  const checkLimit = (type: string, current: number) => {
    // Проверка лимита
  }
  
  return { isPremium, limits, checkLimit }
}
```

#### 2.2. Обновить UI компоненты
- **`pages/settings/index.vue`**: Динамическое отображение статуса
- **`components/TodoModal.vue`**: Предупреждение при достижении лимита
- **`components/ProjectModal.vue`**: Предупреждение при достижении лимита
- Создать компонент `PremiumBadge.vue` для отображения бейджа
- Создать компонент `PremiumUpgradeModal.vue` для предложения апгрейда

#### 2.3. Добавить страницу/модальное окно для премиум
**Файл**: `pages/premium/index.vue` или модальное окно
- Информация о преимуществах премиум
- Способы получения премиум
- Текущий статус и лимиты

---

## 📝 Детальный план реализации

### Этап 1: Backend - Базовая инфраструктура (1-2 дня)

1. **Создать утилиты для работы с премиум**
   - [ ] `server/utils/premium.ts` - функции проверки статуса
   - [ ] `server/utils/limits.ts` - константы лимитов и проверка

2. **Обновить API endpoints для проверки лимитов**
   - [ ] `server/api/todos/index.post.ts` - проверка лимита задач
   - [ ] `server/api/projects/index.post.ts` - проверка лимита проектов
   - [ ] `server/api/tags/index.post.ts` - проверка лимита тегов
   - [ ] `server/api/todos/[id].put.ts` - проверка лимита подзадач
   - [ ] `server/api/todos/index.post.ts` - проверка лимита повторяющихся задач

3. **Создать новые API endpoints**
   - [ ] `server/api/premium/status.get.ts` - получение статуса
   - [ ] `server/api/premium/limits.get.ts` - получение лимитов

4. **Обновить типы**
   - [ ] Добавить `PremiumLimits` в `types/todo.ts`
   - [ ] Добавить `PremiumStatus` в `types/todo.ts`

### Этап 2: Frontend - Композаблы и логика (1 день)

1. **Создать композабл `usePremium`**
   - [ ] `composables/usePremium.ts` - основная логика
   - [ ] Интеграция с Telegram API
   - [ ] Кеширование статуса

2. **Обновить существующие композаблы**
   - [ ] `composables/useTodos.ts` - проверка лимита перед созданием
   - [ ] `composables/useProjects.ts` - проверка лимита перед созданием
   - [ ] `composables/useSettings.ts` - проверка премиум функций

### Этап 3: UI компоненты (2-3 дня)

1. **Обновить страницу настроек**
   - [ ] `pages/settings/index.vue` - динамическое отображение статуса
   - [ ] Условное отображение "Премиум участник" бейджа
   - [ ] Ссылка на страницу премиум

2. **Создать компоненты**
   - [ ] `components/PremiumBadge.vue` - бейдж премиум статуса
   - [ ] `components/PremiumUpgradeModal.vue` - модальное окно апгрейда
   - [ ] `components/PremiumLimitWarning.vue` - предупреждение о лимите

3. **Обновить существующие компоненты**
   - [ ] `components/TodoModal.vue` - проверка лимита задач
   - [ ] `components/ProjectModal.vue` - проверка лимита проектов
   - [ ] `components/TodoItem.vue` - проверка лимита подзадач
   - [ ] `components/RecurrenceSelector.vue` - проверка лимита повторяющихся задач

4. **Создать страницу премиум**
   - [ ] `pages/premium/index.vue` - информация о премиум
   - [ ] Список преимуществ
   - [ ] Текущие лимиты
   - [ ] Способы получения премиум

### Этап 4: Интеграция и тестирование (1-2 дня)

1. **Интеграция проверок лимитов**
   - [ ] Тестирование всех ограничений
   - [ ] Проверка сообщений об ошибках
   - [ ] Проверка UI предупреждений

2. **Тестирование**
   - [ ] Unit тесты для утилит премиум
   - [ ] E2E тесты для проверки лимитов
   - [ ] Тестирование с реальным Telegram Premium аккаунтом

3. **Документация**
   - [ ] Обновить README с информацией о премиум
   - [ ] Добавить комментарии в код
   - [ ] Создать документацию для разработчиков

### Этап 5: Дополнительные функции (опционально, 2-3 дня)

1. **Расширенная статистика для премиум**
   - [ ] Графики продуктивности
   - [ ] Аналитика по проектам
   - [ ] Тренды выполнения задач

2. **Кастомные темы для премиум**
   - [ ] Выбор акцентных цветов
   - [ ] Дополнительные темы оформления

3. **Автоматическое резервное копирование**
   - [ ] Ежедневные бэкапы для премиум
   - [ ] История бэкапов

---

## 🎨 UI/UX рекомендации

### Отображение статуса премиум:
- **Бейдж**: Золотой/фиолетовый значок "⭐ Premium" или "👑 Premium"
- **Расположение**: В карточке профиля (уже есть), в навигации, в модальных окнах при достижении лимита

### Предупреждения о лимитах:
- **Стиль**: Не навязчивые, но заметные
- **Текст**: "Вы достигли лимита бесплатного аккаунта. Получите Premium для неограниченного использования."
- **Действие**: Кнопка "Узнать больше" или "Получить Premium"

### Страница премиум:
- **Дизайн**: Современный, привлекательный
- **Содержание**: 
  - Список преимуществ с иконками
  - Сравнительная таблица Free vs Premium
  - Текущие лимиты пользователя
  - Способы получения премиум

---

## 🔄 Синхронизация статуса

### Автоматическая синхронизация:
1. При авторизации через Telegram - проверка `is_premium`
2. Периодическая проверка (раз в час) статуса из Telegram
3. Обновление в базе данных при изменении статуса

### Ручная синхронизация:
- Кнопка "Обновить статус" в настройках
- Автоматическая проверка при открытии страницы премиум

---

## 📊 Метрики и аналитика

### Отслеживание:
- Количество премиум пользователей
- Конверсия в премиум (если будет собственная система)
- Наиболее часто достигаемые лимиты
- Популярность премиум функций

---

## 🚀 Приоритеты реализации

### Высокий приоритет (MVP):
1. ✅ Базовая проверка премиум статуса
2. ✅ Ограничения на задачи, проекты, теги
3. ✅ UI для отображения статуса
4. ✅ Предупреждения при достижении лимитов

### Средний приоритет:
1. Страница премиум с информацией
2. Ограничения на подзадачи и повторяющиеся задачи
3. Расширенные настройки уведомлений для премиум

### Низкий приоритет (будущие улучшения):
1. Расширенная статистика
2. Кастомные темы
3. Автоматическое резервное копирование
4. Собственная система подписки

---

## 📝 Примеры кода

### Проверка лимита задач (Backend):
```typescript
// server/api/todos/index.post.ts
const isPremium = await checkPremiumStatus(userId)
const currentTodosCount = await getActiveTodosCount(userId)
const maxTodos = isPremium ? Infinity : 50

if (currentTodosCount >= maxTodos) {
  throw createError({
    statusCode: 403,
    message: 'Достигнут лимит задач. Получите Premium для неограниченного использования.'
  })
}
```

### Проверка лимита в UI:
```vue
<!-- components/TodoModal.vue -->
<script setup>
const { isPremium, limits, checkLimit } = usePremium()
const { todos } = useTodos()

const canCreateTodo = computed(() => {
  if (isPremium.value) return true
  const activeTodos = todos.value.filter(t => !t.completed)
  return activeTodos.length < limits.value.maxTodos
})
</script>

<template>
  <div v-if="!canCreateTodo" class="premium-warning">
    <p>Достигнут лимит задач ({{ limits.maxTodos }}). Получите Premium!</p>
  </div>
</template>
```

---

## ✅ Чеклист готовности

- [ ] Backend утилиты для проверки премиум
- [ ] API endpoints с проверкой лимитов
- [ ] Frontend композабл usePremium
- [ ] UI компоненты для отображения статуса
- [ ] Предупреждения при достижении лимитов
- [ ] Страница премиум
- [ ] Тестирование всех ограничений
- [ ] Документация
- [ ] Интеграция с Telegram Premium API

---

## 📚 Дополнительные ресурсы

- [Telegram WebApp API - User](https://core.telegram.org/bots/webapps#initializing-mini-apps)
- [Telegram Premium](https://telegram.org/blog/premium)

---

**Дата создания**: 2024
**Версия**: 1.0
**Статус**: План

